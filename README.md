# ASL La Garenne

Site Jekyll de l'ASL La Garenne.

## Développement local

Installez Jekyll, puis lancez :

```sh
jekyll serve
```

Le site est généré dans `_site/`. Pour publier une actualité, ajoutez un
fichier Markdown daté dans `_posts/` avec le layout `post`.